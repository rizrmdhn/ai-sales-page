{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": [
        "DATABASE_URL",
        "CORS_ORIGIN",
        "NODE_ENV",
        "GROQ_API_KEY",
        "JWT_SECRET",
        "JWT_REFRESH_SECRET",
        "JWT_ACCESS_TOKEN_EXPIRY",
        "JWT_REFRESH_TOKEN_EXPIRY"
      ]
    }
  }
}
